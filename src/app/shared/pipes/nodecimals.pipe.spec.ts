import { NodecimalsPipe } from './nodecimals.pipe';

describe('NodecimalsPipe', () => {
  it('create an instance', () => {
    const pipe = new NodecimalsPipe();
    expect(pipe).toBeTruthy();
  });
});
